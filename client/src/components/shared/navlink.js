import { styled } from '@mui/system';
import { NavLink } from 'react-router-dom';

// TODO: Update all NavLink references to StyledNavLink so it's not confused with react-router-dom's NavLink

/**
 * Custom NavLink element with mui sx styling support
 */
export default styled(NavLink)``;
