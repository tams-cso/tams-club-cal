import { styled } from '@mui/system';

/**
 * The styled span component is a <span /> component
 * with the MUI sx styling system applied to it.
 */
const StyledSpan = styled('span')``;
export default StyledSpan;
